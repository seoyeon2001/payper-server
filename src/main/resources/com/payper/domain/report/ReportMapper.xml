<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.payper.domain.report.ReportMapper">

    <select id="findPayloadByUserAndMonth" resultType="string">
        SELECT payload
        FROM report
        WHERE user_id = #{userId}
          AND month = #{month}
        LIMIT 1;
    </select>

    <insert id="insert">
        INSERT INTO report (user_id, month, payload)
        VALUES (#{userId}, #{month}, CAST(#{payload} AS JSON));
    </insert>
</mapper>
