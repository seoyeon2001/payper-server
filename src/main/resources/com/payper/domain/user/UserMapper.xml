<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.payper.domain.user.UserMapper">

    <select id="findByOauthProviderAndOauthId" resultType="Integer">
        SELECT user_id from user where oauth_provider = #{ oauthProvider } and oauth_id = #{ oauthId }
    </select>

    <select id="findById" resultType="User">
        SELECT * from user WHERE user_id=#{userId} AND is_deleted = false
    </select>

    <update id="updateConnectedId">
        UPDATE user SET connected_id = #{connectedId},last_modified_at=NOW() WHERE user_id = #{userId}
    </update>

    <update id="softDeleteUser">
        UPDATE user SET is_deleted = true, SET deleted_at=NOW(),last_modified_at=NOW() WHERE user_id = #{userId}
    </update>

    <insert id="createUser" useGeneratedKeys="true" keyProperty="user.userId">
        INSERT INTO user (oauth_provider, oauth_id, nickname, last_modified_at)
        values (#{user.oauthProvider}, #{user.oauthId}, #{user.nickname}, NOW())
    </insert>

    <select id="getConnectedId" resultType="String">
        SELECT connected_id FROM user WHERE user_id = #{userId}
    </select>

</mapper>
