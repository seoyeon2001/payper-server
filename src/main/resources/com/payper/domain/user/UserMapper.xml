<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.payper.domain.user.UserMapper">

    <select id="findByOauthProviderAndOauthId" resultType="com.payper.domain.user.domain.User">
        SELECT * from user where oauth_provider = #{ oauthProvider } and oauth_id = #{ oauthId }
    </select>

    <select id="get" resultType="com.payper.domain.user.domain.User">
        SELECT * from user where user_id=#{userId}
    </select>

    <update id="updateConnectedId">
        UPDATE user SET connected_id = #{connectedId},last_modified_at=NOW() WHERE user_id = #{userId}
    </update>

    <update id="softDeleteUser">
        UPDATE user SET is_deleted = true, SET deleted_at=NOW(),last_modified_at=NOW() WHERE user_id = #{userId}
    </update>

    <insert id="createUser">
        <selectKey resultType="Integer" keyProperty="user.userId" keyColumn="user_id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>

        INSERT INTO user (user_id,oauth_provider,oauth_id,nickname,connected_id,created_at,is_deleted,last_modified_at)
        values (null,#{user.oauthProvider},#{user.oauthId},#{user.nickname},#{user.connectedId},NOW(),FALSE,NOW())
    </insert>

    <select id="getUserInfo" resultType="UserResponse">
        SELECT user_id as id, nickname
        FROM user
        WHERE user_id = #{userId}
        AND is_deleted = false
    </select>

</mapper>
